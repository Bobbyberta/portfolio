{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Personal portfolio website",
  "scripts": {
    "build": "mkdir -p dist && cp -r src/* dist/",
    "dev": "npm run build && serve dist"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "devDependencies": {
  }
} 